# ==============================================================================
# ملخص متغيرات البيئة لسكربت تقييم الفيديو عبر Gemini API
# ==============================================================================

# مفتاح API الخاص بـ Google Gemini (مطلوب)
GEMINI_API_KEY=ضع_مفتاح_API_الخاص_بك_هنا

# مسار ملف الفيديو المدخل (افتراضي: /app/data/input_video.mp4)
VIDEO_PATH=./data/input_video.mp4

# مسار التقرير النهائي الناتج (افتراضي: /app/data/qa_report.txt)
OUTPUT_FILE=./data/qa_report.txt

# (اختياري) رابط تحميل مباشر للفيديو من SharePoint/OneDrive
# اتركه فارغاً لو الفيديو موجود محلياً بالفعل
VIDEO_URL=

# (اختياري) مسار ملف الكوكيز في حال كان الرابط محمياً بمصادقة
COOKIES_FILE=./data/cookies.txt

# اسم موديل Gemini المفضل للاستخدام
MODEL_NAME=gemini-3.5-flash

# وضع معالجة الوسائط - القيم المتاحة:
#   auto           → يحاول الصوت أولاً، وإن فشل يرفع الفيديو (الافتراضي)
#   audio          → صوت فقط، يفشل فوراً إذا تعذر استخراجه
#   video          → فيديو كامل مباشرة، لا يتراجع للصوت أبداً
#   audio_fallback → يرفع الفيديو أولاً، فإن فشل الموديل يجرب الصوت تلقائياً
PROCESSING_MODE=auto

# الإجراء الافتراضي عند تجاوز حد التوكنز (1,000,000) قبل الرفع:
#   1 → اكمل رفع الفيديو كاملاً بالرغم من التجاوز (قد يفشل الموديل)
#   2 → قسّم الفيديو إلى أجزاء (45 دقيقة لكل جزء) وادمج التقارير في النهاية
#   3 → تحويل للصوت فقط - الأسرع والأوفر في الكوتة (الافتراضي)
TOKEN_LIMIT_ACTION=3

# مسار ملف تتبع استهلاك الكوتة التراكمية اليومية ومدى الحياة
USAGE_TRACKER_FILE=./data/gemini_usage_tracker.json
